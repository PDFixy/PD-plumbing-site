import React from 'react';
import Layout from '../../components/Layout';

const leak_detection = () => (
  <Layout>
    <h1>Leak Detection</h1>
    <p>This is the Leak Detection landing page for PD Plumbing.</p>
  </Layout>
);

export default leak_detection;
